{
  "name": "miniwiki-bot",
  "version": "0.1.0",
  "description": "시에라에 붙는 멀티테넌트 봇 서비스의 템플릿",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.test.json",
    "test": "vitest run",
    "heroku-postbuild": "npm run build"
  },
  "license": "UNLICENSED",
  "type": "module",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.20.7",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=22"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
